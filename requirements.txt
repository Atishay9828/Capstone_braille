# Braillix — Python dependencies
# Phase 0 + Phase 1 only. All packages are free/open-source.

# --- Web framework ---
fastapi==0.111.0
uvicorn[standard]==0.29.0

# --- Braille translation (THE core library) ---
python-louis==3.28.0

# --- Data validation ---
pydantic==2.7.1
pydantic-settings==2.2.1

# --- Config / env ---
python-dotenv==1.0.1

# --- Testing ---
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0

# ------------------------------------------------------------
# Phase 2 dependencies — uncomment when starting Phase 2 work.
# These are heavy; do NOT install during Phase 0/1 development.
# ------------------------------------------------------------
# pix2tex==0.1.2          # math image → LaTeX OCR
# pdfplumber==0.11.0      # PDF text extraction
# PyMuPDF==1.24.3         # PDF image extraction (fitz)
